<h1 class="text-center mt-4">Edit category name</h1>

<div class="container">
  <div class="row justify-content-center">
    <div class="col-10">
      <%= render 'form' %>
      <p><%= link_to 'Return to category', category_path(@category), class: "btn btn-outline-secondary" %></p>
    </div>
  </div>
</div>